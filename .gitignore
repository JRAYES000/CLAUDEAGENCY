# Worktrees git temporaires des sous-agents Claude Code (éphémères)
.claude/

# Artefacts transitoires (capture navigateur Playwright MCP)
.playwright-mcp/

# Artefacts transitoires (miniatures Canva téléchargées pour prévisualisation)
.canva-tmp/

# Screenshots de revue (hors versionnement)
/cp-*.png

# Documents de travail internes (audits, plans d'action, netlinking) — hors versionnement
/Audit-SEO-*.docx
/Plan-action-*.docx
/Strategie-LinkedIn-*.docx
/Netlinking-*.xlsx
/AUDIT_CONTENU_*.txt

# Worktrees git locaux temporaires
/worktree-*/

# Archive locale des documents internes (auto-déplacés depuis la racine le 2026-06-26)
/archive/

# Réglages locaux Claude Code (allowlist de permissions perso)
.claude/settings.local.json

# Données personnelles de prospection — jamais commitées
/docs/prive/

# Données de prospection de claudepartners.fr : profils LinkedIn nominatifs,
# e-mails de personnes physiques. Dépôt public — ne jamais versionner ici.
/partenaires-ia/

# Captures de revue et brouillons visuels (même règle que /cp-*.png ci-dessus).
# attest_preview.png contenait une élève nommée et une signature scannée.
/*.png
/.tmp-linkedin/
/app/public/images/

# Modeles d'e-mails d'outreach netlinking : nomment des interlocuteurs tiers.
# Volontairement hors git (decision du 2026-08-21).
/netlinking/outreach-emails.md

# Dossiers et sauvegardes de travail locaux (plans de session, copies de CLAUDE.md).
/.planning/
/CLAUDE.md.bak-*
