# Redirections de CHEMIN relatives uniquement (ex: /ancienne-url /nouvelle-url 301).
# www->apex et *.pages.dev->apex sont geres par des Cloudflare Redirect Rules (Phase 6, Task 6.3),
# car _redirects ne gere pas les redirections au niveau domaine/hostname.

# /sitemap.xml redirige vers le vrai sitemap (Astro API route ne fonctionne pas en mode static)
/sitemap.xml /sitemap-index.xml 301
